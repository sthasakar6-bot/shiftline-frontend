export default function IctAdminComingSoon({ label }: { label: string }) {
  return (
    <div>
      <h1 className="ict-page-title">{label}</h1>
      <p className="ict-page-sub">Not built yet. Ask for it by name and I'll scope it properly.</p>
    </div>
  );
}
